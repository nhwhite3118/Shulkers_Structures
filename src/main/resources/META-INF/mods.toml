modLoader="javafml" #mandatory
loaderVersion="[32,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
#issueTrackerURL="http://my.issue.tracker/" #optional
[[mods]] #mandatory
modId="shulkersstructures" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Shulkers Structures" #mandatory
#updateJSONURL="http://myurl.me/" #optional
# A URL for the "homepage" for this mod, displayed in the mod UI
#displayURL="http://example.com/" #optional
# A file name (in the root of the mod JAR) containing a logo for display
#logoFile="examplemod.png" #optional
credits="A special thanks to TelepathicGrunt for getting me started on structure generation, and everyone in the Minecraft Worldgen discord for always helping out" #optional
authors="britishnickk aka nhwhite3118" #optional
# The description text for the mod (multi line!) (#mandatory)
description='''
This mod is a catch-all for any structures I feel like making that wouldn't fit in a specific mod. They won't necessarily have a vanilla feel to them like in my other mods
'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.cobbler]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.cobbler]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15.2]"
    ordering="NONE"
    side="BOTH"
